<md-sidenav id="sidebar" class="md-sidenav-left" md-component-id="left" md-is-locked-open="$mdMedia('gt-sm')" md-whiteframe="4">
	<header class="nav-header" layout-padding>
		<h1 id="logo" class="md-heading">
			<a href="/">PhalconVM</a>
		</h1>
	</header>

	<md-divider></md-divider>

	<md-content id="sidebar-menu">
		<span class="section-title">Server Environment</span>
		<ul>
			<li ng-repeat="group in menu.environment">
				<span class="group-title">{{group.label}}</span>
				<ul>
					<li class="menu-item" ng-repeat="(key, label) in group.items">
						<a href="#/env/{{key}}">{{label}}</a>
					</li>
				</ul>
			</li>
		</ul>

		<md-divider></md-divider>

		<span class="section-title">Web Sites</span>
		<ul>
		</ul>

		<md-divider></md-divider>

		<span class="section-title">Tools</span>
		<ul>
			<li ng-repeat="group in menu.tools">
				<ul>
					<li class="menu-item" ng-repeat="(key, label) in group.items">
						<a href="#/tools/{{key}}">{{label}}</a>
					</li>
				</ul>
			</li>
		</ul>
	</md-content>
</md-sidenav>

<div layout="column" flex>
	<md-toolbar id="toolbar">
		<div class="md-toolbar-tools">
			<md-button class="md-icon-button" ng-click="toggleSidenav()" hide-gt-sm>
				<md-icon md-svg-src="/svg/ic_menu_black_24px.svg" aria-label="menu" style="color:white"></md-icon>
			</md-button>

			<!-- fill up the space between left and right area -->
			<span flex></span>

			<md-button ng-disabled="true">
				Save Changes
			</md-button>
		</div>
	</md-toolbar>

	<md-content id="content" layout-padding ng-view>
	</md-content>
</div>

<?php echo $this->getContent(); ?>