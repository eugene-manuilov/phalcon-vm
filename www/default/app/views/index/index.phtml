<?php

foreach ( $phalconvm['menu'] as $type => $groups ) :
	foreach ( $groups as $group ) :
		foreach ( $group['items'] as $item => $label ) :
			?><script id="tmpl-<?php echo $type; ?>-<?php echo $item; ?>" type="text/html">
				<header class="header">
					<h2 class="md-display-1"><?php echo $label; ?></h2>
				</header>
				<?php $this->partial( "index/{$type}/{$item}" ); ?>
			</script><?php
		endforeach;
	endforeach;
endforeach;